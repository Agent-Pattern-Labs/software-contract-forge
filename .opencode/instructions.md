# OpenCode Addendum

- Use `/software-contract-forge` as the command router.
- Keep the top-level session focused on routing, verification, and settlement.
- Delegate browser-heavy scan/apply work to `@general-free`.
- Delegate proposal prose, qualification narratives, and nuanced risk analysis to `@general-paid`.
- Use `@glm-minimal` only for small structured extraction tasks.
- Do not spawn recursive tasks from subagents.
