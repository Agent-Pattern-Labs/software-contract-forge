# Data

Consumer projects own runtime data:

- `data/pipeline.md`: pending leads.
- `data/applications/`: dated application/proposal logs.
- `data/raw/`: copied source pages or downloaded RFPs.
- `data/cache/`: reusable fetched artifacts.

This harness package only ships the directory contract.
